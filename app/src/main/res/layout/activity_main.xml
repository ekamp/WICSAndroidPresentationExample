<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.innovi.ekamp.wics.Main">

    <TextView
        android:id="@+id/counter_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:textColor="@color/accent_material_light"
        android:textSize="@dimen/abc_text_size_display_4_material"
        android:text="0"/>

    <Button
        android:id="@+id/incrementing_button"
        android:layout_width="match_parent"
        android:layout_height="96dp"
        android:layout_below="@+id/counter_view"
        android:layout_marginLeft="24dp"
        android:layout_marginRight="24dp"
        android:background="@color/background_material_dark"
        android:padding="8dp"
        android:text="+"
        android:textColor="@color/accent_material_light"
        android:textSize="@dimen/abc_text_size_button_material" />

    <Button
        android:id="@+id/reset_button"
        android:layout_width="match_parent"
        android:layout_height="96dp"
        android:layout_below="@+id/incrementing_button"
        android:layout_marginRight="24dp"
        android:layout_marginLeft="24dp"
        android:layout_marginTop="8dp"
        android:background="@color/background_material_dark"
        android:padding="8dp"
        android:text="RESET"
        android:textColor="@color/accent_material_light"
        android:textSize="@dimen/abc_text_size_button_material" />

</RelativeLayout>
